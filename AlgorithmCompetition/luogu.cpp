#include <iostream>
#include <queue>
using namespace std;

int main() {
  int n, m;
  cin >> n >> m;
  queue<int> myQueue;
  for (int i = 1; i <= n; i++) {
    if (myQueue.empty()) {
      cout << 0 << endl;
    }
    while (!myQueue.empty()) {
    }
  }

  return 0;
}