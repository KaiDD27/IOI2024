#include <fstream>
#include <vector>
#include <algorithm>
#include <map>
#include <cmath>

using namespace std;
int N;
int minX = 400001, minY = 400001, maxX = 0, maxY = 0;
int main()
{
    ifstream fin("reduce.in");
    ofstream fout("reduce.out");
    fout << 12 << endl;
}